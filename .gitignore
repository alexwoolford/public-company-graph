# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Environment
.env
.venv
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/*.log
data/*.log

# Data files (keep structure, ignore large generated files)
data/*.json
data/*.txt
# All SQLite databases (too large for repo, regenerated by pipeline)
data/*.db
# Keep the directory structure
!data/.gitkeep

# Tar backup directory (recovered downloads)
data/tar_backup/

# Cache files (diskcache - SQLite-backed cache with .val files)
# These are automatically regenerated when scripts run
# Contains: 10-K extracted data, company domains, company properties, embeddings
data/cache/

# papers
papers/

# Archive (historical scripts and docs - kept locally for reference)
archive/

# Temporary files
*.tmp
*.bak
*.cache
data/.fingerprints_cache
data/.geoip_cache
data/.user_agent_cache
data/.whois_cache

# SQLite WAL files (write-ahead log)
*.db-shm
*.db-wal

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.cover
.hypothesis/

# Datamule portfolio directories (10-K downloads)
# Legacy: old location in project root
10k_*/
# Datamule fallback directories (created on failed downloads)
fallback_*/
# Test directories created by dev scripts (datamule testing)
test_*/
# Quality test directories created by dev scripts
quality_test_*/
# Datamule portfolio directories - ignore all contents (direct children and recursively)
data/10k_portfolios/*
data/10k_portfolios/**/*
# But keep the directory structure
!data/10k_portfolios/.gitkeep
# Extracted HTML files - ignore all contents (direct children and recursively)
data/10k_filings/*
data/10k_filings/**/*
# But keep the directory structure
!data/10k_filings/.gitkeep

# Test fixtures (include in repo for immediate testability)
!tests/fixtures/
!tests/fixtures/**
# But ignore log files
tests/fixtures/**/*.log

.cursorignore
data/embedding_cache/
